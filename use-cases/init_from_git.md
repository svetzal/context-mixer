OK let's implement the first use-case.

When the user types:

```
pmx init --remote git@github.com:svetzal/prompt-mixer-sample.git
```

The prompt-mixer should clone the repo into its default location.