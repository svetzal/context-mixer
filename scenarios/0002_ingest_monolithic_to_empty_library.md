Given the prompt-mixer library is empty.

When the user types:

```
pmx ingest .junie/guidelines.md
```

Then the prompt-mixer library should contain a copy of the source document in
the file "context.md".