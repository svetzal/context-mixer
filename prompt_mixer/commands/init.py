from rich.panel import Panel


def do_init(console, path, remote, provider, model):
    console.print(
        Panel(f"Initializing prompt library at [bold]{path}[/bold]", title="Prompt Mixer"))
    console.print("This functionality is not yet implemented.")
